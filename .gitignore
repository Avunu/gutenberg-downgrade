vendor
node_modules
.direnv
result*
gutenberg-downgrade.zip

# The Gutenberg 18.5.0 build is fetched by Nix (nix/gutenberg-release.nix) and
# copied here by `nix run .#sync-assets` / the devShell hook. Never commit it.
assets/gutenberg

# Symlink generated by git-hooks.nix on each devShell entry (read by prek);
# points at a system/hooks-hash-specific /nix/store path, so it must stay
# untracked.
.pre-commit-config.yaml

# Test artefacts.
.phpunit.result.cache
tests/audit/.phpstan-cache

cwicly